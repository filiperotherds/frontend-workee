interface createEstimate {
  name: string;
  phone: string;
  email: string;
  cep: string;
  address: string;
  number: string;
}

export async function CreateEstimate({
  name,
  phone,
  email,
  cep,
  address,
  number,
}: createEstimate) {
    
}
